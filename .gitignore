# Build output
build/
*.bin
wakeshift
wakeshift.exe

# Local player data (save files live in the OS user data directory)
*.dat

# Editor / OS noise
.DS_Store
*.swp
.vscode/
.idea/

# Working material, not public docs: design doc, sketches, archived roadmaps.
# Lives on the author's disk only — CLAUDE.md and ROADMAP.md still point at it.
docs/
